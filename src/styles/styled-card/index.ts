import styled from 'styled-components'
import { StyledFlex } from '../styled-flex'

export const StyledCard = styled.div`
  padding: 24px;
  border-radius: 10px;
  box-sizing: border-box;
`

export const StyledFlexCard = styled(StyledFlex)`
  padding: 24px;
  border-radius: 10px;
  box-sizing: border-box;
`
